{
  "name": "first-run",
  "version": "0.1.0",
  "description": "Get any repo running locally. Community-powered setup that gets smarter over time.",
  "type": "module",
  "bin": {
    "first-run": "./dist/bin/first-run.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/bin/first-run.js"
  },
  "keywords": ["cli", "setup", "onboarding", "developer-tools"],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "execa": "^9.5.2",
    "ora": "^8.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
