// T4: setup command — contributor gets personalized setup plan

export async function setupCommand(repoPath: string): Promise<void> {
  // TODO T4: Implement
  // 1. ensureNiaInstalled()
  // 2. scanRepo(repoPath)
  // 3. resolveRequirements(repoPath, scan)
  // 4. profileMachine()
  // 5. Display: repo needs X, you have Y
  // 6. generatePlan(requirements, machine, contradictions)
  // 7. Display plan
  // 8. runPlan(plan) — interactive execution
}
