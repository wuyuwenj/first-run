// T4: learn command — save a fix or tip to the knowledge base

export async function learnCommand(error?: string, fix?: string): Promise<void> {
  // TODO T4: Implement
  // 1. If no error/fix provided, prompt interactively
  // 2. profileMachine() — capture context
  // 3. saveFix({ errorPattern, fix, os, arch, toolVersions })
  // 4. Print confirmation
}
