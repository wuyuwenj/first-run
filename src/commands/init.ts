// T4: init command — maintainer sets up the project knowledge base

export async function initCommand(repoPath: string): Promise<void> {
  // TODO T4: Implement
  // 1. ensureNiaInstalled()
  // 2. indexRepo(repoPath)
  // 3. scanRepo(repoPath) — show what was found
  // 4. Save .first-run.json
  // 5. Suggest adding .claude/skills/onboard/ to repo
  // 6. Print success message
}
