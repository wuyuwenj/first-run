// T4: ask command — query the knowledge base

export async function askCommand(question: string): Promise<void> {
  // TODO T4: Implement
  // 1. ensureNiaInstalled()
  // 2. searchRepo(question)
  // 3. Display cited answer
}
